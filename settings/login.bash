# login.bash - one-time, interactive-only tasks run on each login
# not run for non-interactive ssh commands, for example.

# ========
# Examples
# ========

# # make sure system messages are turned off for ssh logins
# touch $HOME/.hushlogin
